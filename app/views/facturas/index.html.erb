<% content_for :content_header  do %>
  <section class="content-header">
    <h1>Listando <small>Facturas</small></h1>

    <ol class="breadcrumb">
      <li><a href="/"><i class="fa fa-dashboard"></i> Principal</a></li>
      <li class="active">Facturas</li>
    </ol>    
  </section>
<% end %>

<section class="content">
  <div class="row">
    <div class="col-xs-12">
      <div class="box box-primary">
        <div class="box-header">
          <div class="box-tools pull-right">
                    
          </div>
        </div>
        <div class="box-body table-responsive">
          <table class="table table-hover">
            <thead>
              <tr>
                <th>Código de Factura</th>
              <th>Cliente</th>
              <th>Rut de Cliente</th>
              <th>Vendedor</th>
              <th>Rut de Vendedor</th>
                <th>Fecha</th>
                            <th></th>
                <th></th>
                <th></th>
              </tr>
            </thead>
            <tbody>
                        <%
 
contador2 = 0;
%>

              <%= content_tag_for(:tr, @facturas) do |factura| %>
              <td><%= factura.id %></td>
              <td><%= factura.cliente.nombre %></td>
                 <td><%= factura.cliente.rut %></td>
              <td><%= factura.vendedor.nombre %></td>
              <td><%= factura.vendedor.rut %></td>
              <td><%= factura.created_at %></td>
              <% contador2 += 1 %>
                            <td><%= link_to 'Mostrar Factura Completa', factura %></td>
               
              <% end %>
              <tr><th>Facturas Totales</th></tr>
              <td><%= "#{contador2}" %> </td>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</section>

